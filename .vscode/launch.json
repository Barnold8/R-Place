{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Rust",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/r-place/target/debug/r-place.exe",
      "args": [],
      "cwd": "${workspaceFolder}/r-place",
      "stopOnEntry": false,
      "preLaunchTask": "cargo build",
      "console": "integratedTerminal"
    }
  ]
}
